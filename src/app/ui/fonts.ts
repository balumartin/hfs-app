import { Inter, Work_Sans, Cormorant } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const worksans = Work_Sans({ subsets: ["latin"] });

export const cormorant = Cormorant({ subsets: ["latin"] });
